
#先更新 ，后面安装才顺利
conda update -n base -c defaults conda

需要安装的库
matplotlib cartopy gdal netcdf4 eccodes xarray pandas cfgrib h5py hdf4 hdf5 jupyter onnx pillow(PIL) scipy xesmf scikit-learn scikit-image pytorch-lightning tensorboard tqdm torchinfo

pickle是python自带 不用装 ，PIL的库名叫pillow
